<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uniway">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ChallengesScreen"></activity>
        <activity android:name=".Challenge1Screen" />
        <activity android:name=".PhotoViewScreen" />
        <activity android:name=".ARViewScreen" />
        <activity android:name=".TourModeScreen" />
        <activity android:name=".SettingsScreen" />
        <activity android:name=".GroupScreen" />
        <activity android:name=".ProfileScreen" />
        <activity android:name=".MenuScreen" />
        <activity android:name=".FriendsScreen"/>
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>